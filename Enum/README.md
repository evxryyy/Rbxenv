## Enum

## What next

- Adding a component
	- `EnumConstructor.remove` -> `EnumComponent.remove`
	-  Added `EnumComponent.fromValue`, Get the enum from the value..
	-  `EnumComponent.__enum` will containt all enum of the current object
	- Easier way to strictly-type your enums.

## Logs

Version 1.1
- change `type` EnumIndex<Name = string> to `export type`

Version 1.0
- Released the modulea
